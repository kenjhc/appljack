name: 🚀 Deploy Appljack on production server

on:
  push:
    branches:
      master
  pull_request:
    branches:
      master

jobs:
  build:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
    - uses: actions/checkout@v2
    - uses: shivammathur/setup-php@v2
      with:
        php-version: '7.3'
        tools: composer
    - run: composer install
    - uses: actions/setup-node@v1
      with:
        node-version: '12'
    - run: npm install yarn
    - run: yarn install
    - run: yarn build
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@3.1.1
      with:
        ftp-server: 69.167.170.128
        ftp-username: proddeploy@appljack.com
        ftp-password: App1j@ck_dev01$
        local-dir: ./
        git-ftp-args: --dry-run
